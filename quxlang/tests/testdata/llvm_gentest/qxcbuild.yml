linux-x64:
  platform: linux
  cpu: x64
  backend: llvm
  backend_llvm_options:
    mode: debug
  modules:
    main:
      source: main

linux-arm64:
  platform: linux
  cpu: ARM64
  backend: llvm
  backend_llvm_options:
    mode: debug
  modules:
    main:
      source: main

LANGUAGE QUXLANG EN 0.0;

::module_import_scope_test DOC <$ Module import aliases declared in another source file remain available to declarations in this file. $>
DUAL_TEST
{
  VAR text containers::string := "module scope";
  TEST_ASSERT(text == "module scope");
  TEST_ASSERT(first_library::imported_function() == 42);
  TEST_ASSERT(import_lookup_tests::read_imported() == 42);
  TEST_ASSERT(second_library::alias_exports::iterator::read_private_member() == 53);
}

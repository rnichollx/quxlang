LANGUAGE QUXLANG EN 0.0;

IMPORT foolib;
IMPORT foolib AS aliased_foolib;
IMPORT std;

::z_architecture_predicate_test INCLUDE_IF(ARCH_IS_Z_ARCH) UNIT_TEST
{
    TEST_ASSERT(ARCH_IS_Z_ARCH);
    TEST_ASSERT(ARCH_IS_X64 == FALSE);
    TEST_ASSERT(ARCH_IS_ARM64 == FALSE);
}

::multiple_imports_test STATIC_TEST
{
    TEST_ASSERT(foolib::imported_function() == 42);
    TEST_ASSERT(aliased_foolib::imported_function() == 42);
}
